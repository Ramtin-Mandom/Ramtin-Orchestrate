# Agent instructions

- Work only on the requested milestone.
- Keep modules small and independently testable.
- Never hardcode challenge answers.
- Never expose API keys or secrets in code, logs, documentation, or commits.
- Run tests after changes: `python -m pytest -q`.
- Run lint checks: `python -m ruff check .`.
- Preserve unrelated files and user changes. Add no later-milestone features
  unless explicitly requested.
