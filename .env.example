# Milestones 1–12 require no environment variables or API keys.
# Never put real secrets in this example file.

# Milestone 13 extraction only; read from the process environment.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
